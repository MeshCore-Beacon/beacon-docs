# fail2ban filter: beacon-api-429
# Requests to /api/ that beacon-server already throttled (status 429). Only persistence is punished.
[Definition]
failregex = ^<HOST> \S+ \S+ \[?\]? ?"[A-Z]+ /api/[^"]*" 429
ignoreregex =
